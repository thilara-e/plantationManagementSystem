// Only need to change user and password. this is the default settings for xampp.
var DBconfigurations = {
  connectionLimit : 2,
  host     : 'localhost',
  port     : '3306',
  database : 'pms',
  user     : 'root',
  password : ''
}

module.exports = DBconfigurations;